import React from 'react';

export default function Header({children}) {
    return (
        <header>
            {children} 
        </header>
    );
}
// <header> 
//      {children} //{children} header안에 있는 모든데이터를 넣는다
// </header>

